/* Imports */
import type { JSX } from "react";

// ----------------------------------------------------------------------

/**
 * Loader component for full screen loading
 *
 * @component
 * @returns {JSX.Element}
 */
const FullScreenLoader: React.FC = (): JSX.Element => {
  return <div>FullScreenLoader Loading...</div>;
};

export default FullScreenLoader;
